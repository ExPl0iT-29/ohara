## Tasks

- [x] Sanitize and preserve HTML in `ReadabilityExtractor` instead of stripping tags
- [x] Strip HTML before word-counting in `compute_reading_time`
- [x] Render HTML content in `ReaderBody` via `react-native-render-html`, styled with design tokens
- [x] Verify backend test suite still passes (46/46)
- [x] Rebuild and install release APK for manual verification
